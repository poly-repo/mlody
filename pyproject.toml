[project]
name = "Omega"
classifiers = ["Private :: Do Not Upload"]
version = "0"

# Dependencies should NOT use "Version Matching clauses" (matching operator `==`)
# https://packaging.python.org/en/latest/specifications/version-specifiers/#version-matching
# This project uses 'uv pip compile' from https://github.com/astral-sh/uv
# which will ensure the resolved versions of both direct and indirect dependencies are pinned.
# Therefore we only need to constrain versions when some ranges 
# of dependency versions are not compatible.
dependencies = [

  # a funny external dependency we use in smoketests. We use it for
  # tests because hopefully it is not used for anything in production.
  # See: https://pypi.org/project/pyfiglet/
  "pyfiglet",

  # a prossibly temporary LSP multiplexer because eglot doesn't do
  # multiple servers.
  # See: https://github.com/joaotavora/rassumfrassum/tree/master
  "rassumfrassum",

  # a fake file system that mocks the Python file system modules.
  # See: https://github.com/pytest-dev/pyfakefs
  "pyfakefs",

  # a library for nice console output
  # See: https://github.com/Textualize/rich
  "rich",

  # a library for creating CLI applications.
  # See: https://click.palletsprojects.com/en/stable/
  "click",

  # a library for HTTP request handling
  # See: https://pypi.org/project/requests/
  "requests",

  # an advanced Python REPL used for the `mlody shell` interactive session.
  # See: https://github.com/prompt-toolkit/ptpython
  "ptpython",

  # a pythonic generic language server framework used for the mlody LSP server.
  # See: https://github.com/openlawlibrary/pygls
  "pygls",

  # tree-sitter core library — provides Parser, Language, Tree, Node types.
  # Used by the mlody LSP server to parse .mlody (Starlark) documents.
  # See: https://github.com/tree-sitter/py-tree-sitter
  "tree-sitter",

  # Starlark grammar for tree-sitter.
  # See: https://github.com/nickel-lang/tree-sitter-starlark
  "tree-sitter-starlark",

  # git repository introspection (branch, commit, etc.) for mlody context.
  # See: https://gitpython.readthedocs.io/
  "gitpython",
  # HuggingFace model hub client for downloading models.
  # See: https://github.com/huggingface/huggingface_hub
  "huggingface_hub",

  # UUID v7 generation (time-ordered UUIDs per RFC 9562 §5.7).
  # Rust-backed drop-in extension to Python's uuid module.
  # See: https://github.com/bdraco/uuid-utils
  "uuid-utils",

  # workspace DAG builder — directed-graph data structures and algorithms.
  # See: https://networkx.org/
  "networkx",

  # interactive graph visualisation support for DAG displays.
  # See: https://matplotlib.org/
  "matplotlib",
  # Qt backend for matplotlib GUI window (precompiled wheels, no C compilation).
  # See: https://pypi.org/project/PyQt6/
  "PyQt6",
  # vectorized matrix math for calibration and geometry reconstruction.
  # See: https://numpy.org/
  "numpy",
  # table-oriented data wrangling for the Hooli basics demo.
  # See: https://pandas.pydata.org/
  "pandas",
  # HTML parsing stack used by pandas.read_html(..., flavor=\"html5lib\").
  # See: https://pypi.org/project/beautifulsoup4/
  "beautifulsoup4",
  # Standards-compliant HTML parser used by pandas.read_html for cached pages.
  # See: https://pypi.org/project/html5lib/
  "html5lib",
  # webcam capture, calibration parsing, GUI display, and pose solving.
  # See: https://opencv.org/
  "opencv-python",
  # realtime landmark inference for face and full-body pose.
  # See: https://ai.google.dev/edge/mediapipe
  "mediapipe",
  # Ultralytics YOLO runtime for YOLOv26 object detection from webcam frames.
  # See: https://github.com/ultralytics/ultralytics
  "ultralytics",
  # Kokoro text-to-speech inference pipeline.
  # See: https://pypi.org/project/kokoro/
  # Keep Kokoro and Misaki pinned to the latest pair that supports Python 3.13.
  "kokoro>=0.7.4,<0.7.5",
  "misaki[en]>=0.7.4,<0.7.5",
  # Chatterbox is Python 3.11-only in this repo; keep its heavy TTS stack in
  # requirements/chatterbox.in so the default Python 3.13 @pip hub stays usable.

  # columnar data I/O for Parquet-backed mlody value entities.
  # See: https://arrow.apache.org/docs/python/
  "pyarrow",

  # physical units parsing for numeric mlody value metadata.
  # See: https://docs.astropy.org/en/stable/units/
  "astropy",

  # in-memory analytical query engine for Parquet-backed mlody SQL queries.
  # See: https://duckdb.org/docs/api/python/overview
  "duckdb",

  # SQL parser and AST library with DuckDB dialect support for SELECT injection.
  # See: https://github.com/tobymao/sqlglot
  "sqlglot",
]

# See https://docs.astral.sh/ruff/configuration/
[tool.ruff]

# see https://docs.basedpyright.com/v1.23.1/configuration/config-files/
[tool.basedpyright]
venvPath = "/home/mav/.local/share"
venv = "lsp-venv"
typeCheckingMode = "strict"
stubPath = "/home/mav/.local/share/lsp-venv-typings"

reportMissingImports = "error"
reportMissingTypeStubs = false

pythonVersion = "3.13.2"
pythonPlatform = "Linux"

# Tasks: mav-515-setf-compatibility-policy

- [ ] Specify the compatibility-adapter interface for non-exact assignments.
- [ ] Specify backend permission decisions for `inplace` and `copy` modes.
- [ ] Add tests for permission denial, copy-only backends, and successful
      compatibility conversion.

# Tasks: mav-515-setf-sql-selection

- [ ] Specify the writable-place contract for `SqlSegment` expansion.
- [ ] Define transaction semantics when one SQL-selected place fails preflight.
- [ ] Add backend-facing tests for deterministic place materialization and bulk
      validation.
